_default: the_app

include ../../settings.mak

APP_NAME=timer_test

include $(APP_FRONT_MAK)

C_SOURCES=port_timer.c

CFLAGS += -DLINUX_TEST_TIMER

include $(APP_BACK_MAK)

test: $(APP_NAME_EXE)
	$(PYTHON3) test_port_timer.py --exe $(APP_NAME_EXE)
